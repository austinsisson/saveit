.div.text-center
  = "Hello #{@user.name}!"
  = image_tag "#{@user.image}"
.div
  - @bookmarks.each_pair do |topic, link|
    %p
      = topic
    %br
    - link.each do |link|
      = link.name
    %br
    %br