.div.bookmark-category
  - @bookmarks.each_pair do |topic, bookmark|
    .col-md-12.topic
      = "##{topic}"
    %br
    - bookmark.each do |link|
      - site = LinkThumbnailer.generate(link.name)
      .col-md-3.bookmark.text-center  
        %h4
          = link_to "#{site.title}", link.name, class: 'livepreview'
          = image_tag "#{site.favicon}"
        %p 
          "#{site.description}"
        = link_to "Delete", bookmark_path, method: :destroy, class: 'btn btn-danger'
        
.col-md-offset-8.preview-window
  %h4
    Live Bookmark Preview
  .thumbnail
    = holder_tag '300x240', 'Hover over link for preview!', 'sky', id: 'holder'